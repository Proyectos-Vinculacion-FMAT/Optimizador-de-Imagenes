Hola, estos son todos los avances y documentos relacionados con el optimizador de imagenes luego pondre los links a todo
Pasos basicos de creacion y revision del entorno virtual

# Crear el entorno (si no está creado)
python3 -m venv .venv

# Activar (zsh / bash)
source .venv/bin/activate

# Verificar que estás en el entorno
which python
python --version

# Instalar paquetes
pip install --upgrade pip
pip install opencv-python

# Salir del entorno
deactivate

Hola
